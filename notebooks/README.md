# Notebooks

`truthlayer_synthetic_proof.ipynb` is a reproducibility notebook for the generated synthetic artifacts. Run `npm.cmd run artifacts:generate` first, then open the notebook from the repository root in Jupyter or VS Code.

The notebook is intentionally a proof of the engineering pipeline, not a benchmark report.
